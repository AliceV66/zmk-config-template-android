on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: zmkfirmware/zmk-build-arm:stable

    strategy:
      fail-fast: false
      matrix:
        include:
          - board: nice_nano_v2
            shield: wired_split_funda
        

    steps:
    
      - name: Checkout ZMK Config
        uses: actions/checkout@v4
        with:
          path: config

      # (Paso de caché eliminado para asegurar una compilación limpia)

      - name: Initialize west
        run: |
          west init -l config
          # He añadido -v (verbose) para que puedas ver lo que descarga
          west update -v

      - name: Build
        run: |
          # --- CORRECCIÓN DE RUTA AQUÍ ---
          # Apuntamos a la subcarpeta "config/config"
          # que es donde tus archivos .conf y .keymap realmente se encuentran.
          west build --pristine -s zmk/app -b ${{ matrix.board }} -- -DSHIELD=${{ matrix.shield }} -DZMK_CONFIG="${{ github.workspace }}/config/config"
        
      - name: Archive firmware
        uses: actions/upload-artifact@v4
        with:
          name: firmware-${{ matrix.board }}-${{ matrix.shield }}
          path: |
            build/zephyr/zmk.uf2
            build/zephyr/zmk.hex